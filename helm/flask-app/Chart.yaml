apiVersion: v2
name: flask-app
description: DevOps Capstone Flask API — Deployment, Service, and HPA
type: application
version: 0.1.0        # chart version (bump when the chart changes)
appVersion: "1.0.0"   # app version (informational)
