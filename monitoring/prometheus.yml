# Prometheus scrape config for the local docker-compose stack.
# "app" resolves via Docker's internal DNS to the app service.
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "capstone-app"
    metrics_path: /metrics
    static_configs:
      - targets: ["app:5000"]
